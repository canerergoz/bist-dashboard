# bist-dashboard

Data-free static shell for a personal, private stock-tracking tool. It contains
NO data, NO signals and NO strategy: everything is fetched client-side from the
owner's own private repository using the visitor's own fine-grained GitHub
token (stored only in the device's localStorage).

Source of truth lives in the private repository; this repo is a deploy copy.
Not investment advice.
